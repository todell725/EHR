# EmberHeart Gallery

Drop image files (`.png .jpg .jpeg .webp .gif`) into this folder and they'll appear
in the **Gallery** tab in the app automatically. No restart needed — just refresh.

Optional: add a caption in `captions.json` keyed by exact filename. Files without an
entry get a prettified version of their filename as the caption.
