# EmberHeart Reborn — backend dependencies (pinned to tested versions).
# Bump deliberately; re-run `pytest` + scripts/playthrough.py after any change.
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1
openai==2.39.0          # pointed at Ollama's OpenAI-compatible /v1 endpoint
httpx==0.28.1           # health checks + native Ollama fallbacks
numpy==2.4.6            # brute-force cosine over embedding BLOBs
python-dotenv==1.2.2

# dev / test
pytest==9.0.3
pytest-asyncio==1.4.0
